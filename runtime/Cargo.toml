[package]
name = "rayzor-runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib", "staticlib"]

[dependencies]
inventory = "0.3"
rayzor-plugin = { path = "../plugin" }

[profile.release]
opt-level = 3
lto = true
