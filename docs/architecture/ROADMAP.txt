╔══════════════════════════════════════════════════════════════════════════════╗
║                    LAMBDA LOWERING MIGRATION ROADMAP                          ║
║                         Visual Implementation Plan                            ║
╚══════════════════════════════════════════════════════════════════════════════╝


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 1: PREPARATION (2 hours, LOW RISK)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 1.1: Create EnvironmentLayout (30 min)                                 │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/environment_layout.rs (NEW)
   
   [✓] Copy code from LAMBDA_IMPLEMENTATION_GUIDE.md Part 1
   [✓] Add to compiler/src/ir/mod.rs:
       pub mod environment_layout;
       pub use environment_layout::{EnvironmentLayout, EnvironmentField};
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 1.2: Add EnvironmentLayout Tests (30 min)                              │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/environment_layout.rs
   
   [✓] Add #[cfg(test)] mod tests { ... }
   [✓] Test field offset calculation
   [✓] Test cast detection for I32 vs I64
   [✓] Test total_size calculation
   
   Test: cargo test -p compiler environment_layout
   Expected: All tests pass


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 1.3: Enhanced IrBuilder Methods (45 min)                               │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/builder.rs
   
   [✓] Add register_local() helper method
   [✓] Update build_load() to auto-track types
   [✓] Update build_cast() to auto-track types
   [✓] Add infer_binop_type() helper
   [✓] Update build_binop() to auto-track types
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 1.4: Builder Auto-Tracking Tests (15 min)                              │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/builder.rs
   
   [✓] Test build_load registers type
   [✓] Test build_cast registers type
   [✓] Test build_binop registers type
   
   Test: cargo test -p compiler builder
   Expected: All tests pass

   ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
   ┃ PHASE 1 COMPLETE: Infrastructure ready, no existing code changed ┃
   ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 2: REFACTORING (3 hours, LOW RISK)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 2.1: Add Context Structures (20 min)                                   │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy LambdaContext struct from guide
   [✓] Copy SavedLoweringState struct from guide
   [✓] Add save_state() method
   [✓] Add restore_state() method
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 2.2: Extract generate_lambda_skeleton (40 min)                         │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy generate_lambda_skeleton() from guide
   [✓] Refactor to use EnvironmentLayout
   [✓] Return LambdaContext
   [✓] Add inline tests
   
   Test: cargo test -p compiler lambda_skeleton
   Expected: All tests pass


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 2.3: Implement infer_lambda_return_type (45 min)                       │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy infer_lambda_return_type() from guide
   [✓] Implement Strategy 1: Terminator inspection
   [✓] Implement Strategy 2: Body result
   [✓] Implement Strategy 3: CFG scan
   [✓] Add comprehensive tests
   
   Test: cargo test -p compiler infer_return_type
   Expected: All tests pass (mock functions)


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 2.4: Implement validate_lambda_consistency (30 min)                    │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy validate_lambda_consistency() from guide
   [✓] Check all returns match signature
   [✓] Generate clear error messages
   [✓] Add tests
   
   Test: cargo test -p compiler validate_lambda
   Expected: All tests pass


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 2.5: Implement finalize_lambda_terminator (30 min)                     │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy finalize_lambda_terminator() from guide
   [✓] Handle explicit returns
   [✓] Handle expression returns
   [✓] Handle void returns
   [✓] Add tests
   
   Test: cargo test -p compiler finalize_terminator
   Expected: All tests pass

   ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
   ┃ PHASE 2 COMPLETE: All helper functions ready and tested      ┃
   ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 3: INTEGRATION (4 hours, MEDIUM RISK)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 3.1: Implement lower_lambda_body (60 min)                              │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Copy lower_lambda_body() from guide
   [✓] Use EnvironmentLayout.load_field()
   [✓] Remove manual locals.insert() calls
   [✓] Call infer_lambda_return_type()
   [✓] Call validate_lambda_consistency()
   [✓] Call finalize_lambda_terminator()
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 3.2: Replace generate_lambda_function (45 min)                         │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] BACKUP old generate_lambda_function() (rename to _old)
   [✓] Replace with two-pass version from guide:
       1. Call generate_lambda_skeleton()
       2. Call lower_lambda_body()
   [✓] Keep debug logging
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 3.3: Run Individual Lambda Tests (60 min)                              │
└─────────────────────────────────────────────────────────────────────────────┘
   
   Test Order:
   [✓] No captures, explicit return
   [✓] No captures, expression return
   [✓] With captures (I32), explicit return
   [✓] With captures (object), field access
   [✓] With parameters and captures
   
   For EACH test:
      1. Run test
      2. Check Cranelift IR signature
      3. Verify type consistency
      4. If fails: add debug logging, identify issue
      5. Fix and re-test
   
   Test: cargo test -p compiler lambda_tests
   Expected: All 5 tests pass


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 3.4: Run Full E2E Test Suite (60 min)                                  │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/examples/test_rayzor_stdlib_e2e.rs
   
   [✓] Run: cargo run -p compiler --example test_rayzor_stdlib_e2e
   [✓] Monitor output for each test:
       - thread_spawn_basic
       - thread_spawn_qualified
       - thread_multiple
       - channel_basic
       - mutex_basic
       - arc_basic
   
   Expected Results:
      Before: 2/6 passing ❌
      After:  6/6 passing ✅
   
   If ANY test fails:
      1. Check Cranelift IR signature
      2. Check debug logs for type inference
      3. Verify EnvironmentLayout correct
      4. Check validation errors
      5. Fix and re-test


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 3.5: Regression Testing (15 min)                                       │
└─────────────────────────────────────────────────────────────────────────────┘
   
   [✓] Run: cargo test -p compiler
   [✓] Verify NO regressions in existing tests
   [✓] Check compilation time impact
   
   Expected: No regressions, <10% slower compilation

   ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
   ┃ PHASE 3 COMPLETE: Two-pass architecture live, 6/6 passing   ┃
   ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 4: CLEANUP (1 hour, LOW RISK)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 4.1: Remove Debug Code (15 min)                                        │
└─────────────────────────────────────────────────────────────────────────────┘
   
   Files: All modified files
   
   [✓] Remove or make conditional eprintln! debug statements
   [✓] Keep informational logs (Info: Generated lambda...)
   [✓] Add #[cfg(debug_assertions)] where appropriate
   
   Test: cargo build -p compiler --release
   Expected: Clean build, no warnings


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 4.2: Delete Backup Code (10 min)                                       │
└─────────────────────────────────────────────────────────────────────────────┘
   
   File: compiler/src/ir/hir_to_mir.rs
   
   [✓] Delete generate_lambda_function_old() if exists
   [✓] Remove commented-out old code
   [✓] Clean up unused imports
   
   Test: cargo build -p compiler
   Expected: Compiles successfully


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 4.3: Update Documentation (20 min)                                     │
└─────────────────────────────────────────────────────────────────────────────┘
   
   Files:
   [✓] compiler/src/ir/hir_to_mir.rs: Update module doc comment
   [✓] compiler/src/ir/environment_layout.rs: Add usage examples
   [✓] compiler/src/ir/builder.rs: Document auto-tracking
   [✓] Add MIGRATION_NOTES.md with:
       - What was changed
       - Why it was changed
       - How the new system works
   
   Review: Check all public APIs documented


┌─────────────────────────────────────────────────────────────────────────────┐
│ Step 4.4: Performance Profiling (15 min)                                    │
└─────────────────────────────────────────────────────────────────────────────┘
   
   Test: cargo build -p compiler --release
         time cargo run -p compiler --example test_rayzor_stdlib_e2e
   
   [✓] Compare before/after compilation time
   [✓] Expected: <10% slower
   [✓] If >10%: Profile hotspots, optimize if needed
   [✓] Document performance impact

   ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
   ┃ PHASE 4 COMPLETE: Production-ready code                     ┃
   ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 SUCCESS CRITERIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   ✅ All 6 stdlib E2E tests passing (100%)
   ✅ No Cranelift verifier errors
   ✅ No type mismatches
   ✅ No regressions in existing tests
   ✅ Code complexity reduced (no manual locals tracking)
   ✅ Compilation time impact <10%
   ✅ All code documented
   ✅ Clean cargo build (no warnings)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ROLLBACK PLAN (If Issues Arise)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   BEFORE PHASE 3:
      git checkout -b lambda-lowering-two-pass
      git commit -m "Backup before two-pass migration"

   IF PHASE 3 FAILS:
      git diff HEAD > two-pass-attempt.patch
      git reset --hard HEAD
      
      Review issues:
      1. Check test output for specific failures
      2. Review Cranelift IR for type mismatches
      3. Verify EnvironmentLayout correctness
      4. Check infer_return_type logic
      
      Fix and retry, or revert:
      git checkout main

   ALL PHASES INCLUDE CHECKPOINTS:
      After each major step, commit:
      git add .
      git commit -m "Phase X Step Y complete"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ESTIMATED TIMELINE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Phase 1: 2 hours   ████████░░░░░░░░░░  20%
   Phase 2: 3 hours   ████████████░░░░░░  30%
   Phase 3: 4 hours   ████████████████░░  40%
   Phase 4: 1 hour    ████░░░░░░░░░░░░░░  10%
   ─────────────────────────────────────────
   TOTAL:   10 hours  ████████████████████ 100%

   Recommended schedule:
      Day 1 Morning:   Phase 1 (2h)
      Day 1 Afternoon: Phase 2 (3h)
      Day 2 Morning:   Phase 3 (4h)
      Day 2 Afternoon: Phase 4 (1h)

   TOTAL: 1.5 days for complete implementation


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 RESOURCES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Documentation:
      /tmp/LAMBDA_LOWERING_ARCHITECTURE_ANALYSIS.md   (Detailed analysis)
      /tmp/LAMBDA_ARCHITECTURE_COMPARISON.txt         (Visual comparison)
      /tmp/LAMBDA_IMPLEMENTATION_GUIDE.md             (Implementation code)
      /tmp/SUMMARY.txt                                (Executive summary)
      /tmp/ROADMAP.txt                                (This document)

   Code to Copy:
      Part 1: EnvironmentLayout (150 lines)
      Part 2: Builder methods (80 lines)
      Part 3: Context structs (40 lines)
      Part 4: Two-pass generation (250 lines)
      Part 5: Tests (100 lines)
      ─────────────────────────────────
      TOTAL: ~620 lines of code

   Test Suite:
      5 unit tests for lambda patterns
      6 E2E tests (stdlib)
      Builder auto-tracking tests
      EnvironmentLayout tests


╔══════════════════════════════════════════════════════════════════════════════╗
║                              READY TO BEGIN                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

Start with: Phase 1, Step 1.1 - Create EnvironmentLayout
Document: LAMBDA_IMPLEMENTATION_GUIDE.md Part 1

Good luck! 🚀

